server {
    listen 80;
    server_name _;

    location /api/ {
        proxy_pass http://aligngpt-api:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Request-ID $request_id;
    }
}
