# Fine Tuning

Fine-tuning covers SFT, LoRA/QLoRA, DPO, PPO, and reward model training. Existing configs under `configs/*.yaml` are retained; productionized configs should move toward `configs/model_configs/`.
