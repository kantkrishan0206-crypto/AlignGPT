# Example Data

This directory contains toy JSONL fixtures from the original RLHF prototype. They are suitable for examples only and should not be treated as production data.

Production datasets belong in governed storage and should be represented by manifests under `datasets/dataset_registry/`.
