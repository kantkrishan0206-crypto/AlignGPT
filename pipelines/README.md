# Pipelines

Pipelines coordinate repeatable jobs for training, inference, RAG, indexing, retraining, deployment, and scheduled operations. Each pipeline should define inputs, outputs, owners, retries, metrics, and rollback behavior.
