# Benchmark Documentation

Benchmarks must state task, dataset, sample size, metric definitions, thresholds, baseline, and confidence interval approach.

Smoke benchmarks run in CI. Heavy evaluations should run in scheduled pipelines and publish artifacts under `artifacts/benchmarks/`.
