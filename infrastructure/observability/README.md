# Observability

Observability covers metrics, dashboards, traces, logs, alerts, and benchmark regression signals.

The API emits Prometheus-compatible metrics at `/metrics`, trace events in evaluation responses, and dashboard-ready status via `/v1/status`. Grafana and alerting examples are included for latency, safety findings, reward drift, and backend routing behavior.
