# Benchmark Results

Committed benchmark artifacts provide operational proof for the platform. They are small, deterministic examples generated by `benchmarking/reproducibility/run_operational_benchmark.py`.

Production benchmark runs should publish the same JSON/CSV/Markdown contract to object storage and dashboard ingestion.
