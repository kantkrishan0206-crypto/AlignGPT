# Agent System

The agent system contains planning, memory, tool-use, orchestration, multi-agent, workflow, autonomous-reasoning, and safety-constraint components.

Implementations should be conservative: tools need explicit allowlists, memory needs retention policy, and autonomous workflows need evaluation gates.
