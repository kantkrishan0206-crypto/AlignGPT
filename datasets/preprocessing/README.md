# Dataset Preprocessing

Preprocessing normalizes prompts, responses, preferences, labels, and metadata. Existing helpers in `src/data/build_prompts.py` and `src/data/make_pref_pairs.py` can be migrated here as package APIs.
