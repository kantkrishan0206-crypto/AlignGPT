# Datasets

Dataset workflows are separated into ingestion, preprocessing, vectorization, synthetic generation, quality control, and registry management.

Toy data in `data/` remains for quick examples. Production datasets should be registered here with schema, license, source, and quality metadata.
